# Negative corpus

Negative cases are source shapes that should be rejected with stable diagnostics.
Each case records the expected `LRC` code, source-span requirement, next feature,
tests, and documentation link.
